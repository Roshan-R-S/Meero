# Full local runtime install: base + AI + desktop.
# Artifact publishing clients remain optional in requirements-cloud.txt.
-r requirements-base.txt
-r requirements-ai.txt
-r requirements-desktop.txt

# Hugging Face Hub client for optional model uploads
huggingface-hub==1.0.1
